# MarketMind AI — contexto para Claude Code

Track 5 (Agentic Scale, TAWS/ESPOL). FastAPI + LangGraph + React. Detalle completo en [`DOCS/PLAN.md`](DOCS/PLAN.md) y [`DOCS/DOCUMENTO_TECNICO.md`](DOCS/DOCUMENTO_TECNICO.md). Tests: `pytest` (backend).

## Features recientes

**"El Analista que aprende del Comité"** (feedback humano como few-shot) — completada, commiteada en `main` y verificada funcionando en producción. Detalle completo en `DOCS/PLAN.md` → sección **"Completado: feedback humano como few-shot"** (al final del archivo). Si vas a tocar `backend/agents/` o `backend/services/signals.py`, léela primero para no duplicar ese diseño.
