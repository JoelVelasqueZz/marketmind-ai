import ComingSoon from "../components/ComingSoon";

export default function Watchlist() {
  return (
    <ComingSoon
      title="Watchlist"
      description="Extra opcional (no requerido por las HU mínimas) — vista consolidada por lista de seguimiento."
    />
  );
}
